## GET /api/v1/notifications
Success.

### Example

#### Request
```
GET /api/v1/notifications HTTP/1.1
Accept: text/xml,application/xml,application/xhtml+xml,text/html;q=0.9,text/plain;q=0.8,image/png,*/*;q=0.5
Authorization: Token token="6f4646b58bcd66f0c12fe9ff9218b97430b9a007b1c967e4740a1110174d5c504b0334e8c5e72c7c"
Content-Length: 0
Content-Type: application/x-www-form-urlencoded
Host: www.example.com
```

#### Response
```
HTTP/1.1 200
Cache-Control: max-age=0, private, must-revalidate
Content-Length: 2045
Content-Type: application/json; charset=utf-8
ETag: W/"902962cacc2a07ab0e9ef86f029a928d"
X-Content-Type-Options: nosniff
X-Frame-Options: SAMEORIGIN
X-Request-Id: ee6129be-8e96-4349-afe8-24c1126ccf4f
X-Runtime: 0.016912
X-XSS-Protection: 1; mode=block

{
  "notifications": [
    {
      "id": 106,
      "title": "Try to compress the AI firewall, maybe it will compress the virtual port!",
      "body": "You can't connect the application without bypassing the digital SCSI microchip!",
      "opened": true
    },
    {
      "id": 107,
      "title": "compressing the program won't do anything, we need to calculate the mobile COM protocol!",
      "body": "parsing the pixel won't do anything, we need to input the wireless COM pixel!",
      "opened": true
    },
    {
      "id": 108,
      "title": "I'll index the virtual IB transmitter, that should driver the SCSI interface!",
      "body": "The RAM system is down, navigate the auxiliary program so we can transmit the ADP program!",
      "opened": true
    },
    {
      "id": 109,
      "title": "If we input the circuit, we can get to the SSL hard drive through the wireless SSL bandwidth!",
      "body": "connecting the firewall won't do anything, we need to generate the neural THX panel!",
      "opened": false
    },
    {
      "id": 110,
      "title": "I'll calculate the wireless THX feed, that should alarm the JSON system!",
      "body": "bypassing the card won't do anything, we need to calculate the 1080p XSS panel!",
      "opened": true
    },
    {
      "id": 111,
      "title": "copying the firewall won't do anything, we need to quantify the bluetooth JBOD pixel!",
      "body": "Use the optical AGP hard drive, then you can override the open-source transmitter!",
      "opened": false
    },
    {
      "id": 112,
      "title": "Try to transmit the SCSI firewall, maybe it will program the solid state port!",
      "body": "I'll hack the wireless SSL driver, that should interface the HTTP matrix!",
      "opened": true
    },
    {
      "id": 113,
      "title": "Use the mobile THX matrix, then you can parse the auxiliary transmitter!",
      "body": "I'll connect the redundant PNG sensor, that should microchip the RAM program!",
      "opened": false
    },
    {
      "id": 114,
      "title": "Try to synthesize the GB protocol, maybe it will transmit the optical driver!",
      "body": "I'll back up the primary EXE circuit, that should monitor the SDD interface!",
      "opened": true
    },
    {
      "id": 115,
      "title": "We need to navigate the haptic JSON firewall!",
      "body": "The HTTP matrix is down, copy the redundant system so we can calculate the PNG alarm!",
      "opened": false
    }
  ]
}
```
